# راهنمای شخصی‌سازی پیشرفته سیستم تشخیص تقلب

## ✅ تغییرات انجام شده

سیستم شخصی‌سازی پیشرفته با قابلیت کنترل دقیق بر روی نحوه تشخیص تقلب پیاده‌سازی شده است.

## 🎯 سطوح پیش‌تعریف شده

### 1. Smart Mode (حالت هوشمند)
**قوی‌ترین حالت برای تشخیص تقلب**
- نادیده گرفتن نام متغیرها ✓
- نادیده گرفتن نام توابع ✓  
- حتی اگر دانشجو نام متغیرها و توابع را تغییر دهد، تقلب تشخیص داده می‌شود

### 2. Balanced Mode (حالت متعادل) - پیش‌فرض
**تعادل بین دقت و انعطاف**
- نادیده گرفتن نام متغیرها ✓
- حفظ نام توابع
- مناسب برای بیشتر موارد

### 3. Strict Mode (حالت سخت‌گیرانه)
**فقط کپی‌های مستقیم**
- حفظ تمام شناسه‌ها
- فقط زمانی که کدها کاملاً یکسان باشند تقلب تشخیص داده می‌شود

## 🔧 تنظیمات گرانولار

شما می‌توانید هر یک از این موارد را به صورت جداگانه کنترل کنید:

### شناسه‌ها (Identifiers)
- `ignore_variable_names`: نادیده گرفتن نام متغیرها
- `ignore_function_names`: نادیده گرفتن نام توابع
- `ignore_type_names`: نادیده گرفتن نام انواع (struct, typedef, enum)

### مقادیر (Literals)
- `ignore_string_literals`: نادیده گرفتن رشته‌های متنی
- `ignore_numeric_literals`: نادیده گرفتن اعداد

### ساختاری (Structural)
- `normalize_whitespace`: نرمال‌سازی فاصله‌ها
- `remove_comments`: حذف کامنت‌ها
- `remove_includes`: حذف #include ها

## 📝 نحوه استفاده

### استفاده از حالت‌های پیش‌تعریف شده

```python
from config import SensitivityConfig

# حالت هوشمند (Smart)
config = SensitivityConfig.smart()

# حالت متعادل (Balanced)
config = SensitivityConfig.balanced()

# حالت سخت‌گیرانه (Strict)
config = SensitivityConfig.strict()
```

### استفاده از حالت شخصی‌سازی شده

```python
from config import SensitivityConfig

# مثال: فقط نام متغیرها را نادیده بگیر
config = SensitivityConfig.custom(
    ignore_variable_names=True,
    ignore_function_names=False
)

# مثال: همه چیز به جز اعداد
config = SensitivityConfig.custom(
    ignore_variable_names=True,
    ignore_function_names=True,
    ignore_type_names=True,
    ignore_string_literals=True,
    ignore_numeric_literals=False  # اعداد حفظ می‌شوند
)
```

### استفاده از طریق API

برای استفاده از API، می‌توانید از پارامتر `ignore_variable_names` استفاده کنید:
- `ignore_variable_names=true` → Smart mode (نادیده گرفتن نام متغیرها)
- `ignore_variable_names=false` → Strict mode (حفظ تمام شناسه‌ها)

## 💡 مثال‌های کاربردی

### مثال 1: فقط نام متغیرها را نادیده بگیر
```python
config = SensitivityConfig.custom(
    ignore_variable_names=True,
    ignore_function_names=False,
    ignore_type_names=False
)
```
**نتیجه**: اگر دو دانشجو کدی با نام‌های متغیر مختلف بنویسند ولی منطق یکسان باشد، تشخیص داده می‌شود.

### مثال 2: حالت فوق‌العاده هوشمند
```python
config = SensitivityConfig.smart()
```
**نتیجه**: حتی اگر نام متغیرها، توابع، و حتی ترتیب توابع تغییر کند، تشخیص داده می‌شود.

### مثال 3: حالت سخت‌گیرانه
```python
config = SensitivityConfig.strict()
```
**نتیجه**: فقط کپی‌های مستقیم (با همان نام‌های متغیر و تابع) تشخیص داده می‌شود.

## 🔄 سازگاری با نسخه قبلی

برای سازگاری با کدهای موجود:
- پارامتر `ignore_variable_names` همچنان کار می‌کند
- `ignore_variable_names=True` → معادل Smart mode
- `ignore_variable_names=False` → معادل Strict mode

## ⚙️ تنظیمات پیش‌فرض

در حال حاضر، سیستم از **Balanced Mode** به عنوان پیش‌فرض استفاده می‌کند که تعادل خوبی بین دقت و انعطاف دارد.

## 🎛️ کنترل کامل

با استفاده از `SensitivityConfig.custom()` می‌توانید کنترل کاملی بر روی همه جنبه‌های تشخیص تقلب داشته باشید و تنظیمات را به دلخواه خود تغییر دهید.

---

**نکته**: سیستم جدید بسیار انعطاف‌پذیر است و می‌توانید ترکیب‌های مختلفی از تنظیمات را امتحان کنید تا به سطح مطلوب خود برسید.

