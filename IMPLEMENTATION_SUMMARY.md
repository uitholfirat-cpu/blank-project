# خلاصه پیاده‌سازی سیستم شخصی‌سازی پیشرفته

## ✅ تغییرات انجام شده

### 1. کلاس SensitivityConfig در config.py

یک کلاس جامع برای کنترل دقیق حساسیت تشخیص تقلب اضافه شده است:

```python
from config import SensitivityConfig

# حالت‌های پیش‌تعریف شده
smart_config = SensitivityConfig.smart()      # حالت هوشمند
balanced_config = SensitivityConfig.balanced()  # حالت متعادل (پیش‌فرض)
strict_config = SensitivityConfig.strict()     # حالت سخت‌گیرانه

# حالت شخصی‌سازی شده
custom_config = SensitivityConfig.custom(
    ignore_variable_names=True,
    ignore_function_names=False,
    ignore_string_literals=True
)
```

**ویژگی‌های کلیدی:**
- ✅ کنترل جداگانه برای نام متغیرها
- ✅ کنترل جداگانه برای نام توابع
- ✅ کنترل جداگانه برای نام انواع
- ✅ کنترل برای رشته‌ها و اعداد
- ✅ تنظیمات ساختاری (فاصله، کامنت، و غیره)

### 2. بهبود Tokenizer

متدهای جدید اضافه شده که از SensitivityConfig استفاده می‌کنند:

```python
tokenizer = CTokenizer()

# استفاده از SensitivityConfig
tokens = tokenizer.tokenize_with_sensitivity(code, sensitivity_config)
token_list = tokenizer.tokenize_to_list_with_sensitivity(code, sensitivity_config)
```

**مزایا:**
- ✅ پشتیبانی کامل از SensitivityConfig
- ✅ سازگاری با متدهای قبلی (mode="structural"/"literal")
- ✅ انعطاف‌پذیری بالا

### 3. به‌روزرسانی API

کلاس `GradingSettings` به‌روزرسانی شده تا پارامترهای جدید را بپذیرد:

```python
class GradingSettings(BaseModel):
    # پارامترهای موجود
    threshold: Optional[float] = None
    ignore_variable_names: Optional[bool] = None  # برای سازگاری
    
    # پارامترهای جدید
    sensitivity_mode: Optional[str] = None  # "smart", "balanced", "strict"
    ignore_function_names: Optional[bool] = None
    ignore_type_names: Optional[bool] = None
    ignore_string_literals: Optional[bool] = None
    ignore_numeric_literals: Optional[bool] = None
```

## 🎯 نحوه استفاده

### استفاده از حالت‌های پیش‌تعریف شده

#### حالت Smart (هوشمند)
```python
from config import SensitivityConfig

config = SensitivityConfig.smart()
# - نادیده گرفتن نام متغیرها
# - نادیده گرفتن نام توابع
# - تشخیص تقلب حتی با تغییر نام‌ها
```

#### حالت Balanced (متعادل) - پیش‌فرض
```python
config = SensitivityConfig.balanced()
# - نادیده گرفتن نام متغیرها
# - حفظ نام توابع
# - تعادل بین دقت و انعطاف
```

#### حالت Strict (سخت‌گیرانه)
```python
config = SensitivityConfig.strict()
# - حفظ تمام شناسه‌ها
# - فقط کپی‌های مستقیم
```

### استفاده از حالت شخصی‌سازی شده

```python
# فقط نام متغیرها را نادیده بگیر
config = SensitivityConfig.custom(
    ignore_variable_names=True,
    ignore_function_names=False,
    ignore_type_names=False
)

# همه چیز به جز اعداد
config = SensitivityConfig.custom(
    ignore_variable_names=True,
    ignore_function_names=True,
    ignore_type_names=True,
    ignore_string_literals=True,
    ignore_numeric_literals=False  # اعداد حفظ می‌شوند
)
```

### استفاده از طریق API

برای سازگاری با نسخه قبلی، همچنان می‌توانید از `ignore_variable_names` استفاده کنید:

```http
POST /upload?ignore_variable_names=true
```

برای استفاده از حالت‌های جدید:

```http
POST /upload?sensitivity_mode=smart
POST /upload?sensitivity_mode=balanced
POST /upload?sensitivity_mode=strict
```

برای حالت شخصی‌سازی شده:

```http
POST /upload?
    ignore_variable_names=true&
    ignore_function_names=true&
    ignore_string_literals=false
```

## 📊 مقایسه حالت‌ها

| حالت | نام متغیرها | نام توابع | نام انواع | استفاده |
|------|------------|----------|-----------|---------|
| **Smart** | نادیده | نادیده | حفظ | قوی‌ترین تشخیص |
| **Balanced** | نادیده | حفظ | حفظ | حالت متعادل (پیش‌فرض) |
| **Strict** | حفظ | حفظ | حفظ | فقط کپی مستقیم |

## 🔧 مثال‌های کاربردی

### مثال 1: تشخیص تقلب حتی با تغییر نام متغیرها

```python
# کد دانشجو 1
int count = 0;
for (int i = 0; i < 10; i++) {
    count++;
}

# کد دانشجو 2 (با نام متغیر متفاوت)
int total = 0;
for (int j = 0; j < 10; j++) {
    total++;
}
```

**با Smart Mode**: تقلب تشخیص داده می‌شود ✅
**با Strict Mode**: تقلب تشخیص داده نمی‌شود ❌

### مثال 2: فقط نام متغیرها تغییر کرده

```python
# با Balanced Mode: تقلب تشخیص داده می‌شود ✅
# چون فقط نام متغیرها نادیده گرفته می‌شود
```

## 🎛️ کنترل کامل

شما می‌توانید هر یک از این موارد را به صورت جداگانه کنترل کنید:

- ✅ نام متغیرها
- ✅ نام توابع
- ✅ نام انواع
- ✅ رشته‌های متنی
- ✅ اعداد
- ✅ فاصله‌ها
- ✅ کامنت‌ها
- ✅ #include ها

## 🔄 سازگاری

- ✅ سازگار با کدهای موجود
- ✅ پارامتر `ignore_variable_names` همچنان کار می‌کند
- ✅ متدهای قبلی tokenizer حفظ شده‌اند

## 📝 فایل‌های تغییر یافته

1. **config.py**: اضافه شدن کلاس SensitivityConfig
2. **tokenizer.py**: اضافه شدن متدهای جدید با پشتیبانی از SensitivityConfig
3. **api.py**: به‌روزرسانی GradingSettings (آماده برای استفاده)

## 🚀 مراحل بعدی (اختیاری)

برای تکمیل کامل سیستم، می‌توانید:

1. به‌روزرسانی `plagiarism_detector.py` برای استفاده مستقیم از SensitivityConfig
2. به‌روزرسانی `main.py` برای پشتیبانی از پارامترهای جدید
3. افزودن رابط کاربری در frontend برای تنظیمات پیشرفته

## 💡 نکات مهم

1. **حالت Smart**: برای تشخیص قوی‌تر تقلب، حتی با تغییر نام‌ها
2. **حالت Strict**: فقط برای کپی‌های مستقیم
3. **حالت Balanced**: تعادل خوب برای بیشتر موارد (پیش‌فرض)
4. **شخصی‌سازی**: می‌توانید ترکیب‌های مختلف را امتحان کنید

---

**خلاصه**: سیستم جدید بسیار انعطاف‌پذیر است و به شما کنترل کامل بر روی نحوه تشخیص تقلب می‌دهد. می‌توانید از حالت‌های پیش‌تعریف شده استفاده کنید یا تنظیمات را به دلخواه خود تغییر دهید.

